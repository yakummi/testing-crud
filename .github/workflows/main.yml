
name: Test Project
on:
  push:
     branches:
       - master

jobs:
  test-action:
     runs-on: ubuntu-latest
     steps:
       - name: Run test.py
         run: python /testing-crud/Getting to know the API using the Django REST framework example/smart_home/measurement/tests.py
                      
